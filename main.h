#ifndef HEADER
#define HEADER
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>

int _putchar(char  c);
int str(char *c);
int _printf(const char *format, ...);
int print_string(const char *str);

#endif
